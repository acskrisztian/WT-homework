
const LanguageSelector = ({onChangeDirection}) => {
  return (
	<button onClick={onChangeDirection} className="btn btn-primary rounded-5 py-2 px-3">Change language</button>

  )
}

export default LanguageSelector